import { CheckCircle2, CircleAlert, TrendingUp } from 'lucide-react'
import { Sheet } from '../../ui/Sheet'

export function ProbabilityExplanation({ open, probability, person, onClose }: { open: boolean; probability: number; person: string; onClose: () => void }) {
  return <Sheet open={open} onClose={onClose} title={`Stage signal: ${probability}`}><p className="text-xs text-fg-muted">Calculated for {person} from the current lead record.</p><div className="mt-5 space-y-2">{[{ icon: CheckCircle2, tone: 'text-success bg-success-subtle', title: 'Pipeline stage', detail: 'Later stages increase the score.' }, { icon: TrendingUp, tone: 'text-success bg-success-subtle', title: 'Recent activity', detail: 'Recent customer activity increases the score; older activity reduces it.' }, { icon: CircleAlert, tone: 'text-warn bg-warn-subtle', title: 'Open work', detail: 'A next action increases the score; an objection reduces it.' }].map((reason) => <div key={reason.title} className="flex gap-3 rounded-lg bg-surface-sunk p-3"><span className={['flex h-8 w-8 shrink-0 items-center justify-center rounded-md', reason.tone].join(' ')}><reason.icon aria-hidden size={15} /></span><span><strong className="block text-xs text-fg">{reason.title}</strong><span className="mt-1 block text-xs leading-relaxed text-fg-muted">{reason.detail}</span></span></div>)}</div><p className="mt-4 text-2xs text-fg-subtle">This heuristic is for sorting leads. It is not a forecast or eligibility decision.</p></Sheet>
}
