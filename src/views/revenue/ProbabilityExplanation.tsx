import { CheckCircle2, CircleAlert, Sparkles, TrendingUp } from 'lucide-react'
import { Sheet } from '../../ui/Sheet'

export function ProbabilityExplanation({ open, probability, person, onClose }: { open: boolean; probability: number; person: string; onClose: () => void }) {
  return <Sheet open={open} onClose={onClose} title={`Why ${probability}%?`}><p className="label-caps text-accent">Deal signal · Preview</p><h3 className="mt-3 text-xl font-semibold tracking-[-0.03em] text-fg">A quick prioritisation signal for {person}.</h3><div className="mt-5 space-y-2">{[{ icon: CheckCircle2, tone: 'text-success bg-success-subtle', title: 'Pipeline stage', detail: 'Later stages contribute more confidence than newly captured leads.' }, { icon: TrendingUp, tone: 'text-success bg-success-subtle', title: 'Recent activity', detail: 'A recent customer message strengthens the signal; quiet deals decay over time.' }, { icon: CircleAlert, tone: 'text-warn bg-warn-subtle', title: 'Open work', detail: 'Unresolved objections reduce confidence while a recorded next action improves it.' }].map((reason) => <div key={reason.title} className="flex gap-3 rounded-lg bg-surface-sunk p-3"><span className={['flex h-8 w-8 shrink-0 items-center justify-center rounded-md', reason.tone].join(' ')}><reason.icon aria-hidden size={15} /></span><span><strong className="block text-xs text-fg">{reason.title}</strong><span className="mt-1 block text-xs leading-relaxed text-fg-muted">{reason.detail}</span></span></div>)}</div><div className="mt-5 flex items-center gap-2 rounded-lg border border-border p-3 text-xs text-fg-muted"><Sparkles aria-hidden size={14} className="text-accent" /><span><strong className="text-fg">Use it to sort work,</strong> then read the actual conversation before deciding.</span></div><p className="mt-3 text-2xs text-fg-subtle">Stage-based preview only — not a prediction, guarantee, or eligibility decision.</p></Sheet>
}
