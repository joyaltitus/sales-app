import { CheckCircle2, CircleAlert, Sparkles, TrendingUp } from 'lucide-react'
import { Sheet } from '../../ui/Sheet'

export function ProbabilityExplanation({ open, probability, person, onClose }: { open: boolean; probability: number; person: string; onClose: () => void }) {
  return <Sheet open={open} onClose={onClose} title={`Why ${probability}%?`}><p className="label-caps text-accent">Copilot explanation · Preview</p><h3 className="mt-3 text-xl font-semibold tracking-[-0.03em] text-fg">{person} is moving, with one clear risk.</h3><div className="mt-5 space-y-2">{[{ icon: CheckCircle2, tone: 'text-success bg-success-subtle', title: 'Decision-maker engaged', detail: 'A parent joined the last call and opened the fee breakdown.' }, { icon: TrendingUp, tone: 'text-success bg-success-subtle', title: 'High-intent behavior', detail: 'Three replies and one document open in the last 48 hours.' }, { icon: CircleAlert, tone: 'text-warn bg-warn-subtle', title: 'Price is still open', detail: 'The two-instalment option has not been explicitly accepted.' }].map((reason) => <div key={reason.title} className="flex gap-3 rounded-lg bg-surface-sunk p-3"><span className={['flex h-8 w-8 shrink-0 items-center justify-center rounded-md', reason.tone].join(' ')}><reason.icon aria-hidden size={15} /></span><span><strong className="block text-xs text-fg">{reason.title}</strong><span className="mt-1 block text-xs leading-relaxed text-fg-muted">{reason.detail}</span></span></div>)}</div><div className="mt-5 flex items-center gap-2 rounded-lg border border-border p-3 text-xs text-fg-muted"><Sparkles aria-hidden size={14} className="text-accent" /><span><strong className="text-fg">Next:</strong> confirm the instalment fit on the next call.</span></div><p className="mt-3 text-2xs text-fg-subtle">Estimate only — never a guarantee or eligibility decision.</p></Sheet>
}

